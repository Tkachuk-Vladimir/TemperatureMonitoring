@model IEnumerable<Temperature>

<div>
    @if (Model.Any())
    {
      @foreach (var entity in Model)
            {
               <h4>@entity.Temperatura</h4>
               <p>@entity.DateAdded</p>
            }      
    }
</div>